{
  "name": "payroll",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n back,front -c yellow,cyan \"npm --prefix back run dev\" \"npm --prefix front run start\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}